<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.icode.prespotapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Main_Menu"
            android:label="@string/title_activity_main__menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Login" />
        <activity android:name=".Beranda" />
        <activity android:name=".Karyawan" />
        <activity android:name=".InsertKarya" />
        <activity android:name=".UDKaryawan" />
        <activity android:name=".Presensi" />
        <activity android:name=".DataPresensi" />
        <activity android:name=".Pesan" />
        <activity android:name=".chatRoom" />
        <activity android:name=".Approval" />
        <activity android:name=".BottomNavigator" />
        <activity android:name=".ChatRoomKaryawan" />
        <activity android:name=".PresensiKarya" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".PresensiDataLapang" />
        <activity android:name=".PresensiKaryaKantor" />
        <activity android:name=".UDPresensiKarya" />
        <activity android:name=".UDApproval" />
        <activity android:name=".MenuAdmin" />
        <activity android:name=".PesanAdmin" />
        <activity android:name=".ChatRoomAdmin" />
        <activity android:name=".PresensiDataAdmin" />
        <activity android:name=".SeePresensi"></activity>
    </application>

</manifest>